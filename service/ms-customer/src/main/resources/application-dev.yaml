spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:-localhost:5432}/${DB_NAME:customer}
    username: ${DB_USER:-postgres}
    password: ${DB_PASSWORD:-postgres}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true